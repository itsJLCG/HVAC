import React from 'react';

// Landing page removed from routes to avoid build errors caused by
// the landing-page integration. Components are kept in the repo
// in case you want to re-enable later.

export default function LandingReact() {
  return null;
}
